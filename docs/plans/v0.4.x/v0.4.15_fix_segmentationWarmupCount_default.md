# 実装プラン: v0.4.15 — `segmentationWarmupCount` ドキュメントデフォルト不一致修正

---

## 0. このドキュメントの位置づけ

- 目的: `openclaw.plugin.json` に記載の `segmentationWarmupCount` デフォルト値 `20` が、実際のランタイムデフォルト `10` と不一致なのを修正する
- 深刻度: **MEDIUM** — ユーザーがデフォルト値を明示せず設定した場合、ドキュメント（スキーマ）では `20` と期待されるが実際は `10` で動く。実害は軽微（ウォームアップが短くなるだけでセグメンテーション自体は機能する）が、ユーザーの信頼を損なう
- 発見元: `docs/incidents/v0.4.14_config_pipeline_regression_audit.md` セクション3
- ソースコード検証: **済**

---

## 1. 問題定義

### 現在の不一致

| 場所 | 値 | 備考 |
|------|-----|------|
| `openclaw.plugin.json` L64 | `"Default: 20"` | ユーザーに表示されるスキーマ記述 |
| `config.ts` L81 | `?? 10` | 実際のランタイムデフォルト |
| `segmenter.ts` L78 | `?? 10` | セグメンター側のフォールバック |
| `index.ts` L335-337 | （TypeBox、デフォルト記述なし） | スキーマ側に default 値の記載なし |

### 経緯

v0.4.0 Phase 3 でウォームアップターン数が `20` → `10` に短縮された（短い会話でも早く適応セグメンテーションが有効になるように）。コード側は正しく `10` に更新されたが、`openclaw.plugin.json` の description 内 `"Default: 20"` が更新漏れとなっている。

### 証拠

| # | ファイル | 行 | 内容 |
|---|----------|-----|------|
| 1 | `openclaw.plugin.json` | L64 | `description: "...Default: 20.\n⚠️..."` — デフォルト値の記述が古い `20` のまま |
| 2 | `config.ts` | L81 | `segmentationWarmupCount: rawConfig?.segmentationWarmupCount ?? 10` — 実際のランタイムデフォルトは `10` |
| 3 | `segmenter.ts` | L78 | `this.segmentationWarmupCount = Math.max(0, tuning?.warmupCount ?? 10)` — セグメンター側も `10` |
| 4 | `CHANGELOG.md` | L194, L214 | "Warmup count reduced: `segmentationWarmupCount` default changed from 20 → 10" — 変更は記録済み |

---

## 2. 修正内容

### 2.1 `openclaw.plugin.json` — description 内のデフォルト値を `20` → `10` に修正

**変更前** (L62-65):
```json
"segmentationWarmupCount": {
  "type": "integer",
  "description": "How many messages the AI needs to observe to learn your 'chat style' before using dynamic sensitivity. Default: 20.\n⚠️ [Blast Radius] If too high: The AI stays in a dumb 'fixed threshold' mode for way too long. If too low: It finishes learning on skewed data and its slicing logic gets permanently bugged."
},
```

**変更後**:
```json
"segmentationWarmupCount": {
  "type": "integer",
  "description": "How many messages the AI needs to observe to learn your 'chat style' before using dynamic sensitivity. Default: 10.\n⚠️ [Blast Radius] If too high: The AI stays in a dumb 'fixed threshold' mode for way too long. If too low: It finishes learning on skewed data and its slicing logic gets permanently bugged."
},
```

1箇所の `20` を `10` に変更するだけ。コードの変更は一切不要。

### 2.2 他ファイルの確認 — 変更不要

| ファイル | 現在値 | アクション |
|----------|--------|----------|
| `config.ts` L81 | `?? 10` | ✅ 正しい。変更不要 |
| `segmenter.ts` L78 | `?? 10` | ✅ 正しい。変更不要 |
| `index.ts` L335-337 | TypeBox、default 記述なし | ✅ 変更不要（description に default は含まれていない） |
| `types.ts` L73 | `segmentationWarmupCount?: number` | ✅ 型定義のみ。変更不要 |
| `CHANGELOG.md` | 既に `20 → 10` を記録済み | ✅ 変更不要 |

---

## 3. 受け入れ基準

| # | 確認項目 | 確認方法 |
|---|----------|----------|
| 1 | `openclaw.plugin.json` の `segmentationWarmupCount` description が `Default: 10` を示す | `Select-String` |
| 2 | `openclaw.plugin.json` 内に `Default: 20` が存在しない | `Select-String` で `Default.*20` を検索し、`segmentationWarmupCount` に紐づくものが0件 |
| 3 | `config.ts` L81 が `?? 10` のままである（変更されていない） | `Select-String` |
| 4 | `npm run build:ts` PASS | コマンド実行 |

---

## 4. リスク評価

- **リスク**: 極低。description 文字列1箇所の `20` → `10` 修正のみ。コード動作への影響はゼロ
- **ユーザー影響**: なし。実行時のデフォルト値は既に `10` であり、ドキュメントが実態に合うだけ
- **ロールバック**: 1文字の差分で復元可能

---

## 🔧 Pro Engineer Review — 2025-07-18
> Perspective: Google / IBM Production Engineering
> Principles applied: YAGNI · KISS · DRY · SOLID
> Source code verified: ✅ (as of 2025-07-18)

### 📍 Current Reality (Source Code vs. Document)
- ✅ Document matches code: `openclaw.plugin.json` L64 に `Default: 20` が存在（バグ本身の確認）
- ✅ Document matches code: `config.ts` L81 が `?? 10`（ランタイムデフォルトは正しく 10）
- ✅ Document matches code: `segmenter.ts` L78 が `?? 10`（セグメンター側も正しく 10）
- ✅ Document matches code: `index.ts` TypeBox の `segmentationWarmupCount` description に default 記述なし
- ✅ Document matches code: `CHANGELOG.md` に 20→10 変更記録済み
- ⚠️ Discrepancy found: `openclaw.plugin.json` の他フィールド（`lexicalRebuildIntervalDays`, `recallQueryRecentMessageCount`, `maxPoolChars`, `segmentationTimeGapMinutes`）は JSON Schema の `"default"` プロパティを持つが、`segmentationWarmupCount` にはない。プランは description 内 `Default: 20` の修正のみ言及し、`"default": 10` プロパティの追加に言及していない

### 🎯 Core Problem (1 sentence)
> `openclaw.plugin.json` の `segmentationWarmupCount` description に `Default: 20` と記載されているが、実際のランタイムデフォルトは `10`（v0.4.0 Phase 3 で短縮済み）。ユーザーがドキュメントを信頼してデフォルト値を誤認する。

### 🔍 Principle Filter
| Check | Result | Note |
|-------|--------|------|
| YAGNI — Is this actually needed now? | ✅ Yes | ユーザーのデフォルト値誤認を防ぐ |
| KISS — Is there a simpler solution? | ✅ Simplest possible | description の 20→10 変更のみ |
| DRY — Any duplication to eliminate? | ⚠️ Found | 他フィールドは `"default"` プロパティ + description 内 `Default: N` の二重記載。`segmentationWarmupCount` は description のみで `"default"` プロパティなし — 一貫性に欠ける |
| SOLID — Any violation causing real problems? | ✅ None | ドキュメント修正のみ |

### 🛤️ Solution Options

#### Option A — プラン通り description のみ修正 *(推奨)*
**Approach**: `openclaw.plugin.json` L64 の `Default: 20` → `Default: 10` に変更のみ
**Implementation cost**: Low（1箇所の1文字変更）
**Risk**: 極低（コード動作への影響ゼロ）
**Why recommended**: プランの目的を最小修正で達成。`"default"` プロパティ追加は別改善タスク

#### Option B — description 修正 + `"default": 10` プロパティ追加
**Approach**: description 修正に加えて JSON Schema `"default": 10` プロパティを追加（他フィールドとの一貫性確保）
**Implementation cost**: Low
**Risk**: 低 — `"default"` プロパティは UI/ウィザードで初期値表示に使われる可能性。ランタイム動作には影響なし（`config.ts` が制御）
**When to choose this instead**: 一貫性を重視する場合。ただしスコープ拡張に注意

### ✅ Pro Recommendation
> **Choose Option A because**: プランの目的は「ドキュメントとコードの不一致修正」であり、`Default: 20` → `Default: 10` で完全に達成される。`"default"` プロパティの追加は一貫性の観点から有益だが、本質的には別の改善タスク。
> Estimated implementation: 1分
> Rollback plan: 1文字の差分で復元可能

### ⚡ Quick Wins (implement regardless of option chosen)
- [ ] `segmentationWarmupCount` に `"default": 10` JSON Schema プロパティを追加（`lexicalRebuildIntervalDays`, `segmentationTimeGapMinutes` 等との一貫性向上）
- [ ] `index.ts` TypeBox の `segmentationWarmupCount` description に `Default: 10` を追記（現在 default 記述なし。他フィールドは description 内に default を含む）

---

## ✅ PassOff — 2025-07-18
> 実装完了・全検証PASS

### 受け入れ基準チェック

| # | 確認項目 | 結果 | 詳細 |
|---|----------|------|------|
| 1 | `openclaw.plugin.json` の `segmentationWarmupCount` description が `Default: 10` を示す | ✅ PASS | L64: `Default: 10` を確認 |
| 2 | `openclaw.plugin.json` 内に `segmentationWarmupCount` に紐づく `Default: 20` が存在しない | ✅ PASS | `Default: 20` は incident/plan ドキュメント内のみ |
| 3 | `config.ts` L81 が `?? 10` のままである | ✅ PASS | 変更なし |
| 4 | `npm run build:ts` PASS | ✅ PASS | exitCode: 0 |
| 5 | `npm test` 全PASS | ✅ PASS | 全テスト通過 |

### Code Review
- **結果**: ✅ PASS（指摘なし — description 1箇所の修正のみのためレビュー対象が最小）

### 変更サマリー
- `openclaw.plugin.json` L64: `Default: 20` → `Default: 10` に修正（ランタイム実態 `config.ts` / `segmenter.ts` の `?? 10` と整合）
- コード動作への影響: ゼロ（description 文字列のみの変更）
- ロールバック: 1文字の差分で復元可能