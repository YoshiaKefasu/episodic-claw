{
  "id": "episodic-claw",
  "displayName": "Episodic Memory",
  "version": "0.4.30e",
  "description": "Long-term episodic memory for OpenClaw agents. Uses a Sequential Cache-and-Drain architecture to safely split massive chat logs into 64K chunks, then narrativizes them into continuous episodes via OpenRouter — with per-agent continuity, adaptive idle backoff, wake() guarantees, crash-safe durability, and configurable reasoning/thinking control for higher output quality.",
  "kind": "memory",
  "keywords": ["memory", "episodic", "long-term", "rag", "vector-search", "recall"],
  "author": {
    "name": "YoshiaKefasu",
    "url": "https://github.com/YoshiaKefasu"
  },
  "repository": "https://github.com/YoshiaKefasu/episodic-claw",
  "homepage": "https://github.com/YoshiaKefasu/episodic-claw#readme",
  "license": "MPL-2.0",
  "credentials": {
    "GEMINI_API_KEY": {
      "description": "Gemini API key used by the Go sidecar to embed conversation episodes via the Gemini Embedding API. This key must already be present in your OpenClaw environment — the plugin reads it from the environment and never stores it.",
      "required": true,
      "source": "environment"
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "ON/OFF switch for the Episodic Memory engine. Default: true.\n⚠️ [Blast Radius] If OFF: The AI completely loses its long-term memory and acts like a goldfish with complete amnesia."
      },
      "reserveTokens": {
        "type": "integer",
        "description": "The maximum 'brain space' (tokens) reserved strictly for injecting past memories into the system prompt. Default: 2048.\n⚠️ [Blast Radius] If too high: The AI's prompt gets too crowded, leaving no room to process your current question (causing crashes). If too low: The AI becomes forgetful and gives confused, out-of-context answers."
      },
      "autoInjectGuardMinScore": {
        "type": "number",
        "minimum": 0,
        "maximum": 1,
        "description": "Minimum 0..1 score required before degraded HNSW fallback memories may auto-inject into the system prompt. Default: 0.86.\n⚠️ [Blast Radius] If too low: Weak fallback memories can slip into the prompt and derail the current conversation. If too high: Legitimate fallback memories may be suppressed too aggressively."
      },
      "anchorInjectionAssembles": {
        "type": "integer",
        "description": "How many eligible prompt builds may temporarily inject the most recent compaction Anchor + summary. Default: 1. Budget-truncated early returns do not consume this window.\n⚠️ [Blast Radius] If too high: Old compaction scaffolding lingers too long and starts crowding the prompt. If too low: The bridge text can disappear before the next prompt gets a chance to use it."
      },
      "dedupWindow": {
        "type": "integer",
        "description": "How many turns back the engine looks to block duplicate spam messages. Default: 5.\n⚠️ [Blast Radius] If too high: The AI might wrongly ignore repeated but necessary commands. If too low: Your DB gets flooded with double-posts when the network lags."
      },
      "maxBufferChars": {
        "type": "integer",
        "minimum": 500,
        "description": "Deprecated (v0.4.22c): No longer used. 64K token hard cap replaces this. Retained for backward compatibility only. Default: 7200."
      },
      "maxCharsPerChunk": {
        "type": "integer",
        "minimum": 500,
        "description": "Deprecated (legacy-only): max characters per chunk for the old batchIngest path. No longer used in the v0.4.x narrative cache path. Default: 9000. Retained for backward compatibility only."
      },
      "segmentationLambda": {
        "type": "number",
        "description": "The AI's topic-shift sensitivity. Calculated as 'mean + (lambda * std)'. Default: 2.0.\n⚠️ [Blast Radius] If too high: The AI never groups memories even when the topic completely changes. If too low: The AI becomes hyper-sensitive and slices the memory pointlessly just because you used a new word."
      },
      "segmentationWarmupCount": {
        "type": "integer",
        "description": "How many messages the AI needs to observe to learn your 'chat style' before using dynamic sensitivity. Default: 10.\n⚠️ [Blast Radius] If too high: The AI stays in a dumb 'fixed threshold' mode for way too long. If too low: It finishes learning on skewed data and its slicing logic gets permanently bugged."
      },
      "segmentationMinRawSurprise": {
        "type": "number",
        "description": "The absolute minimum 'Surprise Score' required to authorize cutting a new memory boundary.\n⚠️ [Blast Radius] If too high: Slow, natural topic shifts are never saved and become one giant blob. If too low: The AI saves 'Um, okay' as completely separate, spammy memories."
      },
      "segmentationCooldownTurns": {
        "type": "integer",
        "description": "Mandatory wait turns after slicing a memory before the AI is allowed to slice again.\n⚠️ [Blast Radius] If too long: Fast-paced topic changes get merged into high-speed chaos. If too short: The memory slicing misfires back-to-back, creating a hellish spam of 1-turn memories."
      },
      "segmentationStdFloor": {
        "type": "number",
        "description": "A safety net (Standard Deviation floor) to prevent the AI from becoming hyper-sensitive during boring chats.\n⚠️ [Blast Radius] If too low: During a boring chat, dropping a slightly interesting word causes the AI to overreact and snap the memory in half."
      },
      "segmentationFallbackThreshold": {
        "type": "number",
        "description": "The fixed threshold used when the AI hasn't learned your style yet (Warmup) or encounters an error.\n⚠️ [Blast Radius] If too high: Early conversations never get sliced. If too low: Early conversations get minced into dust."
      },
      "recallSemanticFloor": {
        "type": "number",
        "description": "Recall search limit: Any memory with a vector similarity score below this is thrown in the trash.\n⚠️ [Blast Radius] If too high: The AI becomes a perfectionist and throws away useful memories, recalling nothing. If too low: It pulls up irrelevant garbage memories, causing hallucination (lying)."
      },
      "recallUsefulnessClamp": {
        "type": "number",
        "description": "The maximum 'bonus points' a past memory can receive for being useful previously.\n⚠️ [Blast Radius] If too high: One old, viral memory hogs the top spot forever, burying new important memories (Echo Chamber effect). If too low: The AI can't learn from past successes."
      },
      "recallReplayTieBreakMaxBoost": {
        "type": "number",
        "description": "A tiny pity bonus given to old memories that haven't been reviewed in a while (Spaced Repetition).\n⚠️ [Blast Radius] If too high: Dead, useless memories hijack the search results simply because they are 'old'."
      },
      "recallReplayLowRetrievabilityBonus": {
        "type": "number",
        "description": "The final desperate point boost given to a memory right before it gets permanently forgotten (decayed).\n⚠️ [Blast Radius] If too high: Garbage memories get zombified and refuse to die, turning your vector DB into a slum."
      },
      "recallTopicsMatchBoost": {
        "type": "number",
        "description": "Bonus points awarded when your search perfectly matches the memory's tags/topics.\n⚠️ [Blast Radius] If too high: The AI blindly grabs irrelevant memories just because the tags match, acting completely stupid."
      },
      "recallTopicsMismatchPenalty": {
        "type": "number",
        "description": "Point penalty given to a memory that has tags, but completely missed your search criteria.\n⚠️ [Blast Radius] If too high: A crucial memory with slightly mismatched tags gets locked in the DB dungeon forever."
      },
      "recallTopicsMissingPenalty": {
        "type": "number",
        "description": "Point penalty for memories that have ZERO tags (Recommended: 0).\n⚠️ [Blast Radius] If too high: All your old 'tagless' ace memories get blacklisted and excluded from search."
      },
      "sharedEpisodesDir": {
        "type": "string",
        "description": "Disabled and ignored. Shared episode directories are no longer used at runtime."
      },
      "allowCrossAgentRecall": {
        "type": "boolean",
        "description": "Disabled and ignored. Cross-agent recall is no longer used at runtime."
      },
      "tombstoneRetentionDays": {
        "type": "integer",
        "minimum": 1,
        "description": "How many days to wait before physically deleting forgotten (Tombstoned) memories from the disk. Default: 14.\n⚠️ [Blast Radius] If too short: You have zero time to salvage an important memory that got accidentally forgotten. If too long: You waste hard drive space endlessly."
      },
      "enableBackgroundWorkers": {
        "type": "boolean",
        "description": "Enables background maintenance workers: HealingWorker (auto-rebuilds lexical/vector indexes, handles embedding 429 recovery) and replay scheduling. Default: true.\n⚠️ If false: Index auto-repair and embedding rate-limit recovery are disabled. Narrative generation continues normally."
      },
      "lexicalPreFilterLimit": {
        "type": "integer",
        "minimum": 100,
        "description": "The max number of memories the lightning-fast Lexical (N-gram) engine filters before passing them to the heavy HNSW vector math. Default: 1000.\n⚠️ [Blast Radius] If too high: Your CPU burns up attempting heavy vector math on the entire database. If too low: Excellent memories get casually thrown out by the dumb text-filter, ruining search accuracy."
      },
      "recallReInjectionCooldownTurns": {
        "type": "integer",
        "minimum": 0,
        "description": "Minimum total message turns (user + assistant) that must pass before the same recalled episode set may be re-injected into the system prompt. Default: 24 (≈12 user + 12 assistant exchanges). Set to 0 to disable.\n⚠️ [Blast Radius] If too high: Freshly relevant memories won't reappear even when the conversation naturally circles back. If too low: The same memories spam the prompt every turn, wasting tokens and confusing the agent."
      },
      "lexicalRebuildIntervalDays": {
        "type": "integer",
        "minimum": 1,
        "maximum": 30,
        "default": 7,
        "description": "How often the HealingWorker checks for gaps in the Lexical (Bleve) index and auto-rebuilds if >10% of records are missing. Default: 7 days.\n⚠️ [Blast Radius] If too short: Unnecessary background CPU usage. If too long: Lexical search may be degraded for weeks after a migration or corruption."
      },
      "queryExcludedKeywords": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Keywords to exclude from recall queries. Add words here to prevent them from being used in memory search (e.g., common noise words or sensitive topics)."
      },
      "recallQueryRecentMessageCount": {
        "type": "integer",
        "minimum": 1,
        "maximum": 12,
        "default": 4,
        "description": "How many recent user messages are used to build the deterministic recall query (Parse/Rewrite window size, NOT raw latest message). Default: 4.\n⚠️ [Blast Radius] If too high: Query becomes noisy and dilutes semantic intent. If too low: Insufficient context for accurate memory retrieval."
      },
      "openrouterApiKey": {
        "type": "string",
        "description": "OpenRouter API key for narrative episode generation. Falls back to OPENROUTER_API_KEY env var. Leave empty to disable narrative mode.",
        "secret": true
      },
      "narrativeConfig": {
        "type": "object",
        "description": "[v0.4.29c] Unified narrative generation configuration. Applies to both Google (Gemini/Gemma) and OpenRouter providers. Takes precedence over openrouterConfig (deprecated) and flat equivalents.",
        "additionalProperties": false,
        "properties": {
          "model": {
            "type": "string",
            "default": "openrouter/free",
            "description": "Model ID for OpenRouter fallback phases (Round 2). Default: 'openrouter/free'. Example: 'qwen/qwen3-235b-a22b:free', 'deepseek/deepseek-chat-v3-0324:free'. Google models (Gemini/Gemma) are configured automatically and cannot be overridden here."
          },
          "maxTokens": {
            "type": "integer",
            "minimum": 256,
            "maximum": 32768,
            "description": "Optional. Max tokens cap for the narrative summary. Omit to let OpenRouter/model decide (recommended)."
          },
          "temperature": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "default": 0.4,
            "description": "Sampling temperature for narrative generation. Default: 0.4 (factual, consistent)."
          },
          "timeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 300000,
            "default": 30000,
            "description": "HTTP request timeout for OpenRouter API calls in milliseconds. Default: 30000 (30s). For models that take longer to generate (e.g., free-tier inference queues), set to 180000 (3 min) or 300000 (5 min). Values outside [30000, 300000] are clamped automatically.\n\u26a0\ufe0f [Blast Radius] If too high: A stalled request holds a NarrativeWorker slot for the full timeout. Combine with maxRetries=0 to bound total wait time."
          },
          "maxRetries": {
            "type": "integer",
            "minimum": 0,
            "maximum": 5,
            "default": 3,
            "description": "Number of transport-level retries after a transient failure (timeout, 5xx, rate-limit). Default: 3. For strict 3-5 min fail judgment, set to 0 (no retries — one shot only). Values outside [0, 5] are clamped automatically.\n\u26a0\ufe0f [Blast Radius] If 0: A single transient failure immediately fails the narrative item. Recommended only when combined with a long timeoutMs."
          },
          "reasoning": {
            "type": "object",
            "description": "Reasoning/thinking control for narrative generation. Maps to OpenRouter reasoning params and Google thinkingConfig (thinkingLevel/thinkingBudget). Default: enabled=true, effort=high.",
            "additionalProperties": false,
            "properties": {
              "enabled": {
                "type": "boolean",
                "default": true,
                "description": "Enable reasoning/thinking mode. Default: true. Set false to disable reasoning entirely."
              },
              "effort": {
                "type": "string",
                "enum": ["none", "minimal", "low", "medium", "high", "xhigh"],
                "default": "high",
                "description": "Reasoning effort level. Default: high. Higher effort may increase latency and token usage but improve output quality."
              },
              "maxTokens": {
                "type": "integer",
                "minimum": 1,
                "description": "Optional. Max reasoning tokens cap. When set alongside effort, maxTokens takes precedence and effort is dropped."
              }
            }
          }
        }
      },
      "openrouterConfig": {
        "type": "object",
        "description": "[DEPRECATED v0.4.29c] Use narrativeConfig instead. This key is retained for backward compatibility only and will be removed in a future release. Nested fields take precedence over flat equivalents.",
        "additionalProperties": false,
        "properties": {
          "model": {
            "type": "string",
            "description": "Deprecated: use narrativeConfig.model instead."
          },
          "maxTokens": {
            "type": "integer",
            "minimum": 256,
            "maximum": 32768,
            "description": "Deprecated: use narrativeConfig.maxTokens instead."
          },
          "temperature": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Deprecated: use narrativeConfig.temperature instead."
          },
          "timeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 300000,
            "description": "Deprecated: use narrativeConfig.timeoutMs instead."
          },
          "maxRetries": {
            "type": "integer",
            "minimum": 0,
            "maximum": 5,
            "description": "Deprecated: use narrativeConfig.maxRetries instead."
          },
          "reasoning": {
            "type": "object",
            "description": "Deprecated: use narrativeConfig.reasoning instead.",
            "additionalProperties": false,
            "properties": {
              "enabled": {
                "type": "boolean",
                "description": "Deprecated: use narrativeConfig.reasoning.enabled instead."
              },
              "effort": {
                "type": "string",
                "enum": ["none", "minimal", "low", "medium", "high", "xhigh"],
                "description": "Deprecated: use narrativeConfig.reasoning.effort instead."
              },
              "maxTokens": {
                "type": "integer",
                "minimum": 1,
                "description": "Deprecated: use narrativeConfig.reasoning.maxTokens instead."
              }
            }
          }
        }
      },
      "narrativeSystemPrompt": {
        "type": "string",
        "description": "Custom system prompt for narrative generation. Inline text or path to .md/.txt file. Leave empty for default."
      },
      "narrativeUserPromptTemplate": {
        "type": "string",
        "description": "Custom user prompt template. Variables: {previousEpisode}, {conversationText}. Inline text or path to .md/.txt file."
      },
      "maxPoolChars": {
        "type": "integer",
        "minimum": 1000,
        "default": 15000,
        "description": "Deprecated (v0.4.22c): No longer used. Pool is a passive accumulator; boundary is decided by segmenter only. Retained for backward compatibility only. Default: 15000."
      },
      "narrativePreviousEpisodeRef": {
        "type": "boolean",
        "default": true,
        "description": "Pass the full previous episode to the LLM for context continuity. Default: true."
      },
      "segmentationTimeGapMinutes": {
        "type": "number",
        "minimum": 1,
        "default": 15,
        "description": "Time gap threshold (minutes) that controls both (1) automatic segment boundary detection when a time gap is detected between user messages, and (2) idle auto-flush timeout — the delay before an unfinished buffer is automatically finalized when no new messages arrive. Default: 15. One-knob policy: changing this value affects both behaviors equally.\n⚠️ [Blast Radius] If too short: Excessive segmentation breaks conversation flow. If too long: Long pauses don't create natural episode boundaries."
      },
      "narrativeExpectedLanguage": {
        "type": "string",
        "enum": ["ja", "en", "zh", "ko", "id"],
        "description": "Expected output language for narrative generation. When set, the language guard checks that generated narratives match this language. Leave unset (undefined) to disable language checking (auto mode). Default: unset (no language enforcement). Transplanted from Guardrails AI correct_language validator.\n⚠️ [Blast Radius] If set incorrectly: Valid narratives in the wrong language get flagged as mismatches. If unset: No language checking occurs."
      },
      "narrativeLanguageThreshold": {
        "type": "number",
        "minimum": 0,
        "maximum": 1,
        "default": 0.75,
        "description": "Confidence threshold (0..1) for language detection. Only triggers on_fail when the detected language confidence exceeds this threshold and doesn't match the expected language. Default: 0.75. Lower values catch more mismatches but increase false positives. Transplanted from Guardrails AI correct_language threshold parameter.\n⚠️ [Blast Radius] If too low: Short texts may trigger false mismatch warnings. If too high: Genuine language mismatches may be missed."
      },
      "narrativeLanguageOnFail": {
        "type": "string",
        "enum": ["softwarn", "reask", "handoff"],
        "default": "softwarn",
        "description": "Action when narrative language doesn't match expected language. 'softwarn' = log warning but save narrative (observational, no data loss). 'reask' = retry with same model (v0.4.28c). 'handoff' = switch to fallback model (v0.4.28c). Default: softwarn (safe observation mode). Transplanted from Guardrails AI correct_language on_fail, simplified from callable to enum for TS runtime safety.\n⚠️ [Blast Radius] 'reask'/'handoff' in v0.4.28c may cause item loss if all retries fail — use softwarn until confident in your language settings."
      },
      "narrativeGuardMinSentences": {
        "type": "integer",
        "minimum": 1,
        "maximum": 20,
        "default": 3,
        "description": "Minimum sentence count for narrative output. Runtime branches by detected language: CJK (ja/zh/ko) uses this value directly (default 3). Latin (en/id/unknown) uses this value + 1 (default 4). Single variable + runtime branching for KISS. Default: 3.\n⚠️ [Blast Radius] If too high: Valid short narratives get rejected and retried unnecessarily. If too low: Short/low-quality narratives slip through."
      },
      "narrativeGuardMinCjkChars": {
        "type": "integer",
        "minimum": 10,
        "maximum": 500,
        "default": 120,
        "description": "Minimum CJK character count (whitespace stripped) for narrative output. Only applies when detected language is CJK (ja/zh/ko). Default: 120.\n⚠️ [Blast Radius] If too high: Valid concise narratives get rejected. If too low: Very short/empty narratives slip through."
      },
      "narrativeGuardMinLatinWords": {
        "type": "integer",
        "minimum": 5,
        "maximum": 300,
        "default": 80,
        "description": "Minimum Latin word count (whitespace-split) for narrative output. Applies when detected language is Latin (en/id/unknown). Default: 80.\n⚠️ [Blast Radius] If too high: Valid concise narratives get rejected. If too low: Very short/empty narratives slip through."
      }
    }
  },
  "metadata": {
    "clawdbot": {
      "requires": {
        "env": ["GEMINI_API_KEY"]
      },
      "primaryEnv": "GEMINI_API_KEY",
      "type": "runtime"
    }
  }
}
